/**
 * Typed connectors to public data APIs (CKAN, SIMA, INE, AEMET, Catastro).
 * Each connector is generic: it takes the municipality config as input and
 * never hardcodes local values. First connectors land with the demographics
 * page (Fase 0/1).
 */
export {};
